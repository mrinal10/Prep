package adhoc;

public class FindFirstAndLastPositions {

    private static int findFirst(int[] arr, int target, int low, int high) {

        int mid = arr[arr.length/2];
return mid;
    }

    private static int findLast(int[] arr, int target, int low, int high) {

        int mid = arr[arr.length/2];
return mid;
    }

    public static void main(String[] args) {
        int[] arr = {1, 3, 5, 5, 5, 5, 67, 123, 125};
        findFirst(arr, 0, 0, 0);
        findLast(arr, 0, 0, 0);
        int x = 5;
        System.out.println(x);
    }
}
